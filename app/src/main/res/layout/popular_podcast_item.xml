<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="180dp"
    android:layout_height="160dp"
    android:layout_margin="8dp"
    android:clipChildren="false"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false">

        <!-- Extended colored background layer reaching to button area -->
        <View
            android:id="@+id/color_background"
            android:layout_width="match_parent"
            android:layout_height="240dp"
            android:layout_gravity="top"
            android:background="@color/medium_gray" />

        <!-- Podcast cover with padding to show colored background -->
        <ImageView
            android:id="@+id/podcast_cover"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:layout_gravity="top"
            android:layout_margin="8dp"
            android:layout_marginBottom="20dp"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            android:background="@drawable/rounded_corners_8dp"
            android:clipToOutline="true" />

        <!-- Play button at bottom left edge -->
        <ImageButton
            android:id="@+id/play_button"
            android:layout_width="40dp"
            android:layout_height="32dp"
            android:layout_gravity="bottom|start"
            android:layout_margin="4dp"
            android:background="@drawable/bg_pill_gray_translucent"
            android:src="@drawable/ic_play_24dp"
            android:contentDescription="@string/play_label"
            android:scaleType="centerInside"
            android:padding="6dp"
            app:tint="@color/white" />

        <!-- Options button (horizontal dots) at bottom right edge -->
        <ImageButton
            android:id="@+id/options_button"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="4dp"
            android:background="@android:color/transparent"
            android:src="@drawable/dots_horizontal"
            android:contentDescription="@string/overflow_more"
            android:scaleType="centerInside"
            android:padding="6dp"
            app:tint="@color/white" />

    </FrameLayout>

</com.google.android.material.card.MaterialCardView>
